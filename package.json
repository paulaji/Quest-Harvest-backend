{
  "name": "backend",
  "description": "this is the backend for Quest Harvest and the main entry point will be the transpiled typescript stored in the dist as server.js",
  "main": "./dist/server.js",
  "scripts": {
    "start": "nodemon ./src/server.ts"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
